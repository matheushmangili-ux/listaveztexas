-- ============================================
-- Atualiza proximo_cliente para filtrar por setor
-- NOTA: Versão segura com tenant_id está em rpc.sql
-- Este arquivo mantido por histórico — rpc.sql é a fonte da verdade
-- ============================================

-- DEPRECATED: Use a versão em rpc.sql que inclui filtro de tenant_id
-- Este arquivo NÃO deve ser executado após rpc.sql
